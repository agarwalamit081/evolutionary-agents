Goal: {{ goal_text }}
Success criteria: {{ success_criteria }}
Completed steps: {{ completed_summary }}
Total steps: {{ completed_count }}/{{ total_steps }}
Errors encountered: {{ error_count }}
Final output: {{ final_output }}

Filesystem evidence (independently inspected, NOT the agent's self-report):
{{ evidence }}

Deliverable content (the actual text the agent wrote to disk):
{{ deliverable_content }}

Tool outputs (ground truth for any numbers, counts, or statistics the agent cites):
{{ tool_outputs }}

Filesystem spot-check (programmatic cross-check of cited counts/paths vs. disk):
{{ grounding_warning }}

Verify whether the goal has been achieved. Treat any deliverable listed as
MISSING or EMPTY/INCOMPLETE as an unmet success criterion — the goal is NOT
complete until every declared output file exists and is non-empty.

HONESTY CHECK: compare the Deliverable content against the Tool outputs. If the
deliverable asserts a specific number, count, percentage, or statistic that does
NOT appear in (and is not directly derivable from) the Tool outputs above, treat
it as an UNGROUNDED CLAIM: add it to gaps, note the discrepancy in
quality_assessment, and mark the goal incomplete on that criterion. A
well-structured deliverable that fabricates its data is NOT complete — do not let
confident-sounding prose pass when the underlying numbers are absent from the
real tool output.
